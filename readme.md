1. Run vcpkg - install needed packages
```bash
vcpkg.exe install
```
2. Run cmake - generate project files
```bash
cmake --preset Debug
```
3. Run cmake - build project
```bash
cmake --build build
```